#include<iostream>
#include"Header.h"
using namespace std;

int mian() {
	/*
	float** m;
	m = new float* [5];
	for (int i = 0; i < 5; i++) {
		m[i] = new float[5];
		for (int j = 0; j < 5; j++) {
			m[i][j] = 1;
		}
	}

	Matrix M(m);
	cout << M;
	*/
}